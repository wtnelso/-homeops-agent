document.addEventListener("DOMContentLoaded", () => {
  console.log("🏠 Home Base loaded");

  // Re-render Lucide icons in case they didn’t auto-init
  if (window.lucide) {
    lucide.createIcons();
  }

  // Placeholder: this is where dynamic Home Base data will go
});
